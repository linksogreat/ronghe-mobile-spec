# 融和移动端 UI 项目｜部署推送上线行程（简版）

- 技术栈：Vercel + MongoDB Atlas + Express + 静态 HTML/Vue
- 入口文件：`index.html`
- 后端入口：`server.js`

## 日常推送上线
- 修改代码并提交：
  - `git add .`
  - `git commit -m "feat/fix: 描述改动"`
  - `git push`
- Vercel 自动检测 `main` 分支更新并重新部署；访问你的自定义域名或项目域名验证结果。
- 若改动涉及组件 demo 数据（如 `assets/js/components.js` / `data/components.json`），线上页面会优先读取 MongoDB 的 `/api/components` 覆盖本地默认数据；需要同步更新 MongoDB（见“数据初始化/常见问题”）。

## 最近变更（记录）
- 2026-05-19：优化移动端样机导航类 Sidebar/Tabbar（Sidebar 全高占满内容区；Tabbar 对标 Vant4 页面级示例）。

## 首次/关键配置
- 环境变量：
  - 在本地 `.env` 设置：`MONGODB_URI=你的连接串`（不提交到仓库）
- 数据初始化：
  - `node scripts/seed.js`（将 `data/components.json` 与 `data/data.json` 迁入 MongoDB）
- 后端服务与路由：
  - [server.js](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/server.js)
    - 导出实例：`module.exports = app`
    - API：`/api/components`、`/api/data`
    - 先注册 API，再 `express.static`（避免与同名目录冲突）
    - 根路由：返回 `index.html`
- Vercel 配置：
  - [vercel.json](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/vercel.json)
    - builds：`server.js` 使用 `@vercel/node`；同时启用静态文件构建
    - routes：`/api/(.*)` → `/server.js`；`/(.*)` → `/$1`

## 自定义域名（腾讯云 DNSPod）
- Vercel 项目 Settings → Domains 添加你的域名
- 子域名（如 `www`）：添加 CNAME 记录到 `cname.vercel-dns.com`
- 根域名（`@`）：添加 A 记录到 `76.76.21.21`
- 待 DNS 生效后，Vercel 显示 Valid（✅），即可使用 HTTPS 访问

## 常见问题速查
- 国内网络导致 `git push` 失败：
  - 配置代理：`git config --global http.proxy http://127.0.0.1:端口` 与 `https.proxy`，再 `git push`
  - 或改用 SSH：
    - 生成钥匙并添加到 GitHub
    - `git remote set-url origin git@github.com:linksogreat/ronghe-mobile-spec.git`
    - `git push`
  - 或直接在 GitHub 网页端 Upload files 到 `main`
- `Cannot POST /api/data`：
  - 确认已在 [server.js](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/server.js#L19-L22) 注册 `/api/data` 路由
  - `express.static` 放在 API 路由之后
  - Vercel 路由仅让 `/api/*` 进入后端
- 推送后线上样机仍是旧 demo（右侧样机没更新）：
  - 原因：`mobile_preview.html` 会请求 `/api/components`，MongoDB 里的数据会覆盖 `assets/js/components.js` 的默认数据
  - 处理：同步更新 MongoDB（任选其一）
    - 本地执行 `node scripts/seed.js`（需要正确的 `MONGODB_URI`）
    - 或对线上发起 `POST /api/components`，body 为最新的 components JSON（等价于写入 `id=all_components` 的文档）
- 访问 404/无限加载：
  - 入口文件命名为 `index.html`：[index.html](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/index.html)

## 关键文件参考
- [server.js](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/server.js)
- [vercel.json](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/vercel.json)
- [scripts/seed.js](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/scripts/seed.js)
- [models/StyleConfig.js](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/models/StyleConfig.js)
- [api/data/read.js](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/api/data/read.js)
- [api/data/write.js](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/api/data/write.js)
- [index.html](file:///Users/qiyoutong/Documents/TRAE项目集锦/融和_移动端_UI视觉规范/index.html)
