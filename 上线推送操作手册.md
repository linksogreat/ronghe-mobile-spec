# 融和移动端 UI 视觉规范｜上线推送操作手册

本项目线上由两部分共同决定最终展示：

- 静态页面与样机代码：随 GitHub `main` 分支更新，由 Vercel 自动部署
- 组件/样机数据源：页面会请求 `GET /api/components`，MongoDB 中的数据会覆盖本地默认数据（`assets/js/components.js` 的兜底数据）

因此，当改动涉及组件 demo / rules / code（如 `assets/js/components.js`、`data/components.json`）时，必须执行「推送 + 同步 Mongo」两步，才能让线上最终生效。

## 0. 关键域名与入口

- 自定义验收域名（当前）：`https://www.qiaotong.icu/`
- API：
  - `GET https://www.qiaotong.icu/api/components`
  - `POST https://www.qiaotong.icu/api/components`
- 样机入口：
  - `https://www.qiaotong.icu/pages/mobile_preview.html`
  - 组件直达：`https://www.qiaotong.icu/pages/mobile_preview.html?component=<key>`

## 1. 常见改动类型与对应上线动作

### A. 仅改页面/样式/逻辑（不涉及 Mongo 覆盖的数据）

- 涉及文件示例：`pages/*.html`、`assets/css/*`、`assets/lib/*`、`server.js`（非数据）
- 上线动作：只需要 `git push`（Vercel 自动部署）

### B. 改了组件 demo / rules / code（会被 Mongo 覆盖）

- 涉及文件示例：
  - `assets/js/components.js`（本地兜底数据）
  - `data/components.json`（seed 与线上同步的基准）
- 上线动作：必须
  1) `git push`（触发 Vercel）
  2) 同步 Mongo：对目标域名执行「只替换目标 key」的 `POST /api/components`

## 2. 标准上线流程（推荐）

### Step 1：本地改动前置约束（必须）

- 若改 demo：保持 `assets/js/components.js` 与 `data/components.json` 同步一致（同一个组件 key 的 demos/rules/code）
- 严禁把任何密钥写入仓库：
  - 不提交 `.env`
  - 不在文档/代码中粘贴 `MONGODB_URI` / token / 私钥

### Step 2：本地自查（推荐）

1) JSON 结构校验

```bash
python3 -m json.tool data/components.json > /tmp/components.json.check
```

2) 本地启动静态预览（仅用于自查）

```bash
python3 -m http.server 8000
```

打开：

- `http://localhost:8000/pages/mobile_preview.html?component=<key>`

### Step 3：提交并推送（触发 Vercel）

```bash
git status
git add .
git commit -m "feat/fix: 你的改动描述"
git push
```

推送后在 Vercel 等待部署完成，再用线上域名刷新确认静态资源已更新。

## 3. Mongo 同步（只替换目标 key，避免误覆盖全量）

### 3.1 同步原则（必须）

线上 `POST /api/components` 会把请求体整体写入 Mongo 的 `all_components` 文档。

为避免覆盖其它组件的最新内容，必须采用：

- 先 `GET /api/components` 拉取线上全量
- 再用本地 `data/components.json` 中的目标 key 覆盖线上对象的同名 key
- 最后 `POST /api/components` 回写全量

### 3.2 Python 一行同步（推荐）

将 `<KEY>` 替换为你本次改动的组件 key（例如 `calendar` / `overlay` / `loading`）。

```bash
python3 -c "import json,urllib.request;BASE='https://www.qiaotong.icu';KEY='<KEY>';req=urllib.request.Request(BASE+'/api/components',headers={'User-Agent':'ronghe-spec-sync/1.0','Accept':'application/json'});remote=json.loads(urllib.request.urlopen(req,timeout=30).read().decode('utf-8'));local=json.load(open('data/components.json',encoding='utf-8'));remote[KEY]=local[KEY];payload=json.dumps(remote,ensure_ascii=False).encode('utf-8');req2=urllib.request.Request(BASE+'/api/components',data=payload,method='POST',headers={'User-Agent':'ronghe-spec-sync/1.0','Content-Type':'application/json','Accept':'application/json'});resp=urllib.request.urlopen(req2,timeout=30);print('POST status:',resp.status);print(resp.read().decode('utf-8',errors='replace')[:200])"
```

### 3.3 同步多个 key

把 `KEY='<KEY>'` 改成循环（示例仅展示思路）：

```python
for key in ['calendar', 'overlay']:
    remote[key] = local[key]
```

## 4. 首次/关键配置（密钥与环境变量）

### 4.1 MongoDB Atlas

- 本地 `.env`：
  - `MONGODB_URI=...`（禁止提交到仓库）
- Vercel 环境变量：
  - 在 Vercel Project Settings → Environment Variables 配置同名 `MONGODB_URI`

注意：本手册不会记录任何真实连接串/密钥内容，只记录字段名与配置位置。

### 4.2 自定义域名（DNSPod）

Vercel 项目 Settings → Domains 添加域名后：

- 子域名（如 `www`）：CNAME → `cname.vercel-dns.com`
- 根域名（`@`）：A → `76.76.21.21`

## 5. 验收清单（每次上线后）

- 访问 `https://www.qiaotong.icu/` 页面是否正常加载
- 打开样机：`/pages/mobile_preview.html?component=<key>`
  - 界面是否更新到最新
  - 控制台是否有新增报错
  - 与本次改动相关的交互是否可用
- 若本次涉及 Mongo 覆盖的数据：
  - 重新打开页面，确认是“刷新后仍保持新效果”（不是缓存/临时状态）

## 6. 常见问题

### Q1：我 push 了但线上还是旧效果

- 若改动涉及 demo/rules/code，通常原因是 Mongo 覆盖：
  - 必须执行一次「只替换目标 key」的 `POST /api/components`

### Q2：我同步后发现其它组件被覆盖了

- 原因：把本地全量 `data/components.json` 直接整包 POST 覆盖线上
- 修复：按 3.1 的「GET → 覆盖目标 key → POST」方式同步

### Q3：我看到两个域名效果不一致

- 域名不同意味着数据源不同（都指向同一代码，但可能对应不同项目/环境变量）
- 验收必须以最终验收域名为准（当前为 `qiaotong.icu`）

